#include <Arduino.h>
#include <funcoes.h>


void setup() {
  
  int result;

  result = soma(2, 3);
  result = subtrai(2, 3);
  result = multiplica(2, 3);
  result = divide(2, 3);
  
}

void loop() {
  // put your main code here, to run repeatedly:
}

